import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  containerOption:{
      margin: '-16px',
      position: 'absolute',
  },

  containerOptionReference:{
    position: "relative",
  },
});